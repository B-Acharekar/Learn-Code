import React from 'react'

export default function SPAConcept() {
    return (
        <div>
            <h2>Single Page Application (SPA)</h2>
            <p>
                An SPA loads a single HTML page and dynamically updates the content without
                refreshing the page.
            </p>
            <p>
                React helps build SPAs by managing the view and enabling client-side routing.
            </p>
        </div>
    )
}
